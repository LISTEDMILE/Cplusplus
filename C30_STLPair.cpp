#include <iostream>
using namespace std;

int main(){

    // used when we need a pair of values together
    pair<int, int> p = {1, 5};

    // to access them ....
    cout << "P.first : " << p.first;
    cout << "\nP.second : " << p.second;
}